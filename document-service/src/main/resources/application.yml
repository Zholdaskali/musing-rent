server:
  port: 8084

grpc:
  server:
    port: 9094

spring:
  application:
    name: DOCUMENT-SERVICE
  security:
    user:
      name: user
      password: pass
  datasource:
    url: jdbc:postgresql://localhost:5432/microservice_document
    username: postgres
    password: erke
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
    metadata-map:
      gRPC_port: ${grpc.server.port}

# ???????????? Flyway
flyway:
  enabled: false
  locations: classpath:db/migration
  schemas: public